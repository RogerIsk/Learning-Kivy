<MyLayout>
    BoxLayout:
        orientation: "vertical"
        size: root.width, root.height

        padding: 50     # creates space between the buttons alltogether and the edge of the window 
        spacing: 20     # creates space between the buttons


        Button:
            text: "Hello World"

        Button:
            text: "Pause World"
        
        Button:
            text: "Bye World"
            pos_hint: {'center_x': 0.5}
            size_hint: (0.5, 0.5)
            #size_hint: (None, None)
            #width: 200
            #height: 50